# プロジェクト概要

広告でよくある「多人数シューター」風ゲームをpython+pygameで開発するプロジェクト。
詳細な企画は docs/shooter.md を参照（必読）。

**重要**: ゲーム制作自体が最終目的ではない。ゲームが動く形になったら、AI仮想人格キャラと
一緒にプレイ→AIに改良点を提案させる→改良を反映、のループをYouTube等で配信する企画。
改良しやすさ（パラメータの集約、設定ファイル化）を常に優先する。

# 実行方法

```
python main.py              # 通常起動（ESCで終了、ゲームオーバー後はクリック/Rでリスタート）
python main.py --smoke      # 動作確認モード: 300フレーム自動実行後 smoke.png を保存して終了
python main.py --smoke 1800 # フレーム数指定（1800=約30秒）
python tests/test_phase2.py # ロジックテスト（ヘッドレス実行可）
```

# アーキテクチャ

| ファイル | 役割 |
|---|---|
| main.py | エントリポイント、--smoke引数処理 |
| game/config.py | **全調整パラメータを集約**。バランス調整・AI改良提案はまずここを触る |
| game/projection.py | 疑似3D射影。通路座標(u,d)→画面座標変換、奥行き速度補正 |
| game/assets.py | 画像ロード、スケール済みスプライトのキャッシュ（4px単位に丸めてキャッシュ） |
| game/entities.py | Squad(味方部隊)/Bullet/Enemy/アイテム類/Spawner |
| game/game.py | ゲームループ、当たり判定、HUD、ウィンドウスケーリング |

## 重要な設計判断

- **通路座標系**: ゲームロジックは u（横: -1〜+1）、d（奥行き: 0=手前〜1=奥）で統一。
  描画時のみ台形射影（手前幅1086px、奥幅300px）。座標計算を触るときは必ずこの系で行う
- **奥行き速度補正** (projection.depth_speed_factor): 見た目の大きさのγ乗に比例して
  画面上の速度が変わる（手前ほど加速）。γ=DEPTH_SPEED_GAMMA、基準点=DEPTH_SPEED_REF_D。
  γを変えたら所要時間維持のため基準点も調整すること
- **味方のグルーピング**: 総数Nを 100人/10人/1人ユニットに自動分解（例: 235→100×2+10×3+1×5）。
  大きいユニットほど拡大表示し中央に配置。弾ダメージ=ユニット人数×攻撃力。
  部隊幅は画面横幅の SQUAD_MAX_WIDTH_RATIO(1/4) を超えたら間隔を圧縮して重ねる
- **ウィンドウスケーリング**: pygame.SCALED|RESIZABLEはWindowsでリサイズ不可のため不使用。
  論理サーフェス(1086x1448)に描画→毎フレーム手動でウィンドウサイズに拡縮（レターボックス）。
  マウス座標は mouse_logical_x() で論理座標に変換
- **出現管理**: 種類ごとに独立タイマー（SPAWN_INTERVAL_*_MS）。重み方式ではない
- **背景bg2.pngの実寸は1086x1448**（企画書の1488は誤記）

## 確定済み仕様（docs/shooter.mdに未記載のもの）

- 味方が減るのは「未破壊のアイテムボックス(powerup3)に接触したとき」のみ → 総数が1/2になる
- ボックスを弾で破壊すると攻撃力+1（弾1発の威力+1）。ステージ終了まで持続
- ボックスが画面下に抜けてもゲーム継続。敵が画面下到達のみゲームオーバー
- パネル: +n は n=1〜10、×n は n=2〜5 のランダム。弾はパネルを素通りする
- ステージ設定ファイルのy座標は「スクロール進行距離」（y=5000なら開始から5000px進行時に出現）
- 配信用AIエージェントは別途開発済み。定期的に画面キャプチャを解読してコメントする方式のため、
  ゲーム側からのJSON統計出力等は不要

# 進捗状況（2026-06-04時点）

- [x] **初期実装1**: ゲーム骨格（疑似3D通路、マウス追随、自動連射、雑魚敵、ゲームオーバー/リスタート、
      ウィンドウリサイズ対応）→ コミット済み (2fb89e9)
- [x] **初期実装2**: 味方部隊のグルーピング、パワーアップ（+n/×nパネル、要破壊ボックス）、
      一斉射撃、アイテム数値ラベル、HUD拡張 → 実装・テスト済み、**未コミット**
- [x] バランス調整: 降下速度1/3、敵10倍、×n頻度1/3、手前加速（γ=2.0）、部隊幅1/4制限
- [ ] **初期実装3**（次のタスク）: ステージ設定ファイル、ステージクリア処理、複数ステージ連続プレイ、
      中ボス(enemy2, 耐久10〜99)・大ボス(enemy3, 耐久100〜1000)
      ※ ユーザーが面白くなるステージ構成を検討中。設計が来てから着手
- [ ] **配信フェーズ**: プレイ→AI改良提案→反映のループ。改良はAI人格次第
      （画面がピンクになる等の可能性もあり、それも企画のうち）

# 規約

- ドキュメント・コードともUTF-8で保存（shooter.mdは元Shift-JISだったが変換済み）
- コードのコメントは日本語
- バランス調整は game/config.py の定数変更で行い、ロジックに数値を埋め込まない
- 機能追加時は tests/ にヘッドレステストを追加し、--smoke でスクリーンショット確認する
- コミットメッセージは日本語。フェーズ単位でコミット（ユーザー確認後）
